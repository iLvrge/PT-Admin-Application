/*
 * Class names GENERATED from this module's former makeStyles object by
 * scripts/jss-to-css.cjs; the rules live in the stylesheet imported below.
 *
 * useStyles() deliberately stays a function returning { key: className }, so
 * every `const classes = useStyles()` call site is unchanged.
 */
import './styles.css'

export default () => ({
  "dashboard": "pt-queries-dashboard",
  "dashboardWarapper": "pt-queries-dashboard-warapper",
  "container": "pt-queries-container",
  "flexColumn": "pt-queries-flex-column",
  "customIndex": "pt-queries-custom-index",
  "settingContainer": "pt-queries-setting-container",
  "context": "pt-queries-context",
  "customerSearchHeight": "pt-queries-customer-search-height",
  "splitPane": "pt-queries-split-pane",
  "activeCopyRow": "pt-queries-active-copy-row",
  "queryMessage": "pt-queries-message",
});