/*
 * Class names GENERATED from this module's former makeStyles object by
 * scripts/jss-to-css.cjs; the rules live in the stylesheet imported below.
 *
 * useStyles() deliberately stays a function returning { key: className }, so
 * every `const classes = useStyles()` call site is unchanged.
 */
import './styles.css'

export default () => ({
  "updatedAssetContainer": "pt-updated-assests-updated-asset-container",
  "container": "pt-updated-assests-container",
  "heading": "pt-updated-assests-heading",
  "headingExpand": "pt-updated-assests-heading-expand",
  "context": "pt-updated-assests-context",
  "contextExpand": "pt-updated-assests-context-expand",
  "wrapper": "pt-updated-assests-wrapper",
  "wrapperExpand": "pt-updated-assests-wrapper-expand",
  "fixedSpanWidth70": "pt-updated-assests-fixed-span-width70",
  "fixedSpanWidth30": "pt-updated-assests-fixed-span-width30",
});